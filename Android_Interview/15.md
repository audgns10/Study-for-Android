# 클래스와 객체는 무엇인가요?

<br>

* 클래스는 객체를 생성하기 위한 설계도라고 생각하면 됩니다.(예를 들면 자동차라는 클래스를 만들면 그 안에서 여러 기종의 자동차 객체를 생성할 수 있음) / 속성과 동작을 정의합니다.
* 객체는 클래스를 기반으로 생성된 인스턴스 입니다.(자동차라는 클래스 안에서 생성된 현대, 기아 등이 객체가 되는것) / 실제 사용 가능한 인스턴스입니다.